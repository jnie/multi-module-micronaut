mma:
  outbound:
    advice-slip-api:
      url: "https://api.adviceslip.com"

micronaut:
  application:
    name: multi-module-micronaut
  server:
    port: 8080

endpoints:
  health:
    enabled: true
    sensitive: false